<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title><%= title || 'SkillExchange Platform' %></title>
    <script src="https://cdn.tailwindcss.com"></script>
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.0.0/css/all.min.css">
    <link rel="stylesheet" href="/css/styles.css">
    <script>
        tailwind.config = {
            darkMode: 'class',
            theme: {
                extend: {
                    colors: {
                        background: 'hsl(var(--background))',
                        foreground: 'hsl(var(--foreground))',
                        card: 'hsl(var(--card))',
                        'card-foreground': 'hsl(var(--card-foreground))',
                        popover: 'hsl(var(--popover))',
                    }
                }
            }
        }
    </script>
</head>
<body class="min-h-screen bg-gradient-to-br from-blue-900 via-gray-900 to-black text-white">
    <!-- Navigation -->
    <nav class="bg-blue-900/50 backdrop-blur-sm border-b border-blue-800 p-4">
        <div class="container mx-auto flex items-center justify-between">
            <a href="/" class="flex items-center space-x-2">
                <i class="fas fa-exchange-alt text-2xl text-blue-400"></i>
                <span class="text-xl font-bold bg-gradient-to-r from-blue-400 to-cyan-400 bg-clip-text text-transparent">SkillExchange</span>
            </a>
            
            <div class="flex items-center space-x-4">
                <% if (user) { %>
                    <a href="/dashboard" class="px-4 py-2 text-blue-300 hover:text-white transition-colors">Dashboard</a>
                    <a href="/explore" class="px-4 py-2 text-blue-300 hover:text-white transition-colors">Explore</a>
                    <form action="/logout" method="POST" class="inline">
                        <button type="submit" class="px-4 py-2 bg-red-600 hover:bg-red-700 text-white rounded-lg transition-colors">
                            <i class="fas fa-sign-out-alt mr-2"></i>Logout
                        </button>
                    </form>
                <% } else { %>
                    <a href="/login" class="px-4 py-2 text-blue-300 hover:text-white transition-colors">Login</a>
                    <a href="/signup" class="px-4 py-2 bg-blue-600 hover:bg-blue-700 text-white rounded-lg transition-colors">
                        Sign Up
                    </a>
                <% } %>
            </div>
        </div>
    </nav>
