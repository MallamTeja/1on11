{
  "name": "scholarsearch-backend",
  "version": "1.0.0",
  "description": "Backend services for ScholarSearch",
  "main": "dist/index.js",
  "scripts": {
    "dev": "tsc -w -p ../tsconfig.backend.json",
    "build": "tsc -p ../tsconfig.backend.json",
    "start": "node dist/index.js"
  },
  "dependencies": {
    "@google/generative-ai": "^0.24.1",
    "axios": "^1.6.7",
    "dotenv": "^16.3.1"
  },
  "devDependencies": {
    "@types/node": "^20.6.2",
    "typescript": "^5.2.2"
  }
}
